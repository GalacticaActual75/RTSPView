name: Build Windows release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version, for example 1.0.29 or 1.0.30-beta.1"
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Resolve version
        id: version
        shell: pwsh
        run: |
          $version = if ('${{ github.event_name }}' -eq 'workflow_dispatch') { '${{ inputs.version }}' } else { '${{ github.ref_name }}'.TrimStart('v') }
          if ($version -notmatch '^\d+\.\d+\.\d+(-beta\.\d+)?$') { throw "Invalid release version: $version" }
          "value=$version" >> $env:GITHUB_OUTPUT
          "numeric=$($version.Split('-')[0])" >> $env:GITHUB_OUTPUT
          "tag=v$version" >> $env:GITHUB_OUTPUT

      - name: Restore and test
        run: |
          dotnet restore SpotMonitor.sln -r win-x64
          dotnet run --project tests/SpotMonitor.ConfigurationChecks/SpotMonitor.ConfigurationChecks.csproj -c Release

      - name: Check update helper failure reporting
        run: powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/UpdateHelper.Checks.ps1

      - name: Check drawn viewport masks
        run: node tests/shape-editor.checks.cjs

      - name: Check landscape and portrait presets
        run: node tests/wall-layout-presets.checks.cjs

      - name: Publish self-contained applications
        shell: pwsh
        run: |
          dotnet publish src/SpotMonitor.Viewer/SpotMonitor.Viewer.csproj -c Release -r win-x64 --self-contained true --no-restore -o stage/Viewer /p:Version=${{ steps.version.outputs.value }}
          dotnet publish src/SpotMonitor.Controller/SpotMonitor.Controller.csproj -c Release -r win-x64 --self-contained true --no-restore -o stage/Controller /p:Version=${{ steps.version.outputs.value }}

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Build installer
        shell: pwsh
        run: |
          $env:SPOTMONITOR_VERSION='${{ steps.version.outputs.value }}'
          $env:SPOTMONITOR_NUMERIC_VERSION='${{ steps.version.outputs.numeric }}'
          & "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe" installer/SpotMonitor.iss
          if ($LASTEXITCODE -ne 0) { throw "Installer compilation failed." }
          $installer = Get-ChildItem dist/SpotMonitor-Setup-*-win-x64.exe | Select-Object -First 1
          $hash = (Get-FileHash $installer.FullName -Algorithm SHA256).Hash
          "$hash  $($installer.Name)" | Set-Content "dist/$($installer.BaseName).sha256.txt"
          [ordered]@{
            Version = '${{ steps.version.outputs.value }}'
            Installer = $installer.Name
            Sha256 = $hash
            PublishedAt = (Get-Date).ToUniversalTime().ToString('o')
          } | ConvertTo-Json | Set-Content 'dist/update.json'

      - name: Publish GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag='${{ steps.version.outputs.tag }}'
          [string[]]$releaseFlags = if ('${{ steps.version.outputs.value }}' -match '-beta\.') { @('--prerelease', '--latest=false') } else { @('--latest') }
          if ('${{ github.event_name }}' -eq 'workflow_dispatch') {
            gh release create $tag dist/*.exe dist/*.sha256.txt dist/update.json --target '${{ github.sha }}' @releaseFlags --generate-notes --title "SpotMonitor ${{ steps.version.outputs.value }}"
          } else {
            gh release create $tag dist/*.exe dist/*.sha256.txt dist/update.json --verify-tag @releaseFlags --generate-notes --title "SpotMonitor ${{ steps.version.outputs.value }}"
          }
